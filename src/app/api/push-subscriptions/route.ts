import { NextResponse } from "next/server";
import { requireAuthenticatedSupabase } from "@/services/supabase/request";
function sameOrigin(request:Request){const origin=request.headers.get("origin");return !origin||origin===new URL(request.url).origin;}
export async function POST(request:Request){if(!sameOrigin(request))return NextResponse.json({message:"Invalid request origin."},{status:403});try{const body=await request.json() as{endpoint?:unknown;expirationTime?:unknown;keys?:{p256dh?:unknown;auth?:unknown}};if(typeof body.endpoint!=="string"||!body.endpoint.startsWith("https://")||typeof body.keys?.p256dh!=="string"||typeof body.keys.auth!=="string")return NextResponse.json({message:"Invalid push subscription."},{status:400});const{client,userId}=await requireAuthenticatedSupabase();const device=await client.from("devices").insert({user_id:userId,name:"Web device",user_agent:request.headers.get("user-agent")?.slice(0,500)??null}).select("id").single();if(device.error)throw device.error;const saved=await client.from("push_subscriptions").upsert({user_id:userId,device_id:device.data.id,endpoint:body.endpoint,p256dh:body.keys.p256dh,auth:body.keys.auth,expires_at:typeof body.expirationTime==="number"?new Date(body.expirationTime).toISOString():null,disabled_at:null},{onConflict:"user_id,endpoint"});if(saved.error)throw saved.error;return NextResponse.json({ok:true},{status:201});}catch(error){console.error("[push] subscription failed:",error);return NextResponse.json({message:"Push subscription could not be saved."},{status:500});}}
