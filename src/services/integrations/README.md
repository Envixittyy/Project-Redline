# Integrations

Future adapters may live in folders such as `google-calendar`, `blackboard`, `notion`, `obsidian`, and `ai`. Each adapter should preserve source identity and translate provider-specific details before data reaches feature code. Do not add mock integrations in advance.
