export type NotificationType="blackboard_assignment"|"blackboard_deadline_changed"|"blackboard_announcement"|"due_reminder"|"sync_failure"|"daily_digest";
export function notificationDedupeKey(type:NotificationType,sourceId:string,revision:string){return `${type}:${sourceId}:${revision}`;}
export function safeDeepLink(value:string){return /^\/(?!\/)[A-Za-z0-9/_?=&%.-]*$/.test(value)?value:"/";}
export function isQuietHours(now:Date,timeZone:string,start:string|null,end:string|null){if(!start||!end||start===end)return false;const parts=new Intl.DateTimeFormat("en-GB",{timeZone,hour:"2-digit",minute:"2-digit",hourCycle:"h23"}).format(now);return start<end?parts>=start&&parts<end:parts>=start||parts<end;}
export function pushAvailability(input:{serviceWorker:boolean;pushManager:boolean;standalone:boolean;isIOS:boolean;vapidKey:boolean}){if(!input.serviceWorker||!input.pushManager)return{available:false,reason:"unsupported" as const};if(input.isIOS&&!input.standalone)return{available:false,reason:"install_required" as const};if(!input.vapidKey)return{available:false,reason:"server_unconfigured" as const};return{available:true,reason:null};}
export function safeNotificationPayload(input:{title:string;body:string;deepLink:string;dedupeKey:string}){return{title:input.title.slice(0,100),body:input.body.replace(/https?:\/\/\S+/gi,"[link removed]").slice(0,240),url:safeDeepLink(input.deepLink),dedupeKey:input.dedupeKey};}
