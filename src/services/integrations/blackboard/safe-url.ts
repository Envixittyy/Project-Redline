import { isIP } from "node:net";

export class BlackboardUrlError extends Error{constructor(readonly code:string,message:string){super(message);this.name="BlackboardUrlError";}}
export function isPublicAddress(address:string){if(!isIP(address))return false;const lower=address.toLowerCase();if(lower==="::1"||lower==="::"||lower.startsWith("fc")||lower.startsWith("fd")||lower.startsWith("fe8")||lower.startsWith("fe9")||lower.startsWith("fea")||lower.startsWith("feb"))return false;if(lower.startsWith("::ffff:"))return isPublicAddress(lower.slice(7));if(isIP(address)===4){const [a,b]=address.split(".").map(Number);if(a===0||a===10||a===127||a>=224||a===169&&b===254||a===172&&b>=16&&b<=31||a===192&&b===168||a===100&&b>=64&&b<=127||a===198&&b>=18&&b<=19)return false;}return true;}
export function validateFeedUrl(value:string){let url:URL;try{url=new URL(value);}catch{throw new BlackboardUrlError("invalid_url","Enter a valid Blackboard calendar URL.");}if(url.protocol!=="https:"||url.username||url.password||url.port&&url.port!=="443")throw new BlackboardUrlError("unsafe_url","The Blackboard feed must use standard HTTPS.");if(url.hostname==="localhost"||isIP(url.hostname)&&!isPublicAddress(url.hostname))throw new BlackboardUrlError("unsafe_host","That feed host is not allowed.");return url;}
